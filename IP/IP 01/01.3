<inicio>
	// Declaração das variaveis
	inteiro:N,D;
	real:Z;
	
	// Entrada de dados
	imprime:("Qual o numerador da fração a ser resolvida?");
	leia:(N);
	imprime:("Qual o denominador da fração a ser resolvida?");
	leia:(D);
	
	//Processamento de dados
	Z=N*1.0/D
	
	//Saída de dados
	imprime("O resultado da fração em forma decimal é" + Z)
<Fim>	