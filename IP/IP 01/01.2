<inicio>
	// Declaração de variaveis
	real:A,X,Y,Z;
	string:nome;
	
	// Entrada de dados
	imprime("Como você se chama?");
    leia(nome);
    imprime("Qual sua nota na Prova 1?");
    leia(X);	
	imprime("Qual sua nota na Prova 2?");
    leia(Y);	
	imprime("Qual sua nota na Prova 3?");
    leia(Z);	

	//Processamento de dados
	A=(X+Y+Z)/B
	
	//Saída de dados
	imprime("Olá" + nome + ",sua média final nas três provas foi de" + A)
<Fim>	

Obs: Dei uma olhada nas respostas do exercício 1, por isso a diferença entre o 1º e o 2º programa.